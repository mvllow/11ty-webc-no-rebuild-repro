{
  "name": "11ty-webc-no-rebuild-repro",
  "scripts": {
    "dev": "npx @11ty/eleventy --serve",
    "build": "rm -rf ./_site && npx @11ty/eleventy"
  },
  "dependencies": {
    "@11ty/eleventy": "^2.0.0",
    "@11ty/eleventy-plugin-webc": "^0.10.0"
  }
}
